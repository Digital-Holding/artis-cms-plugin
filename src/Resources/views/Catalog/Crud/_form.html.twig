{% from '@SyliusAdmin/Macro/translationForm.html.twig' import translationForm %}
{% form_theme form '@DHArtisCmsPlugin/Form/theme.html.twig' %}

{{ form_errors(form) }}

<div class="ui one column stackable grid">
    <div class="ui segment">
        {{ form_row(form.menu_code) }}
        {{ form_row(form.year) }}

        <h3 class="ui top attached header">{{ 'artis_cms_plugin.ui.url'|trans }}</h3>
        <div class="ui attached segment">
            {{ form_row(form.url) }}
        </div>
    </div>

    {{ translationForm(form.translations) }}
</div>

<div class="ui two column stackable grid">
    <div class="column">
        <h3 class="ui top attached header">{{ 'sylius.ui.image'|trans }}</h3>
        <div class="ui attached segment">
            {{ form_row(form.image, {'label': false}) }}
        </div>
    </div>
    <div class="column">
        <h3 class="ui top attached header">{{ 'bitbag_sylius_cms_plugin.ui.file'|trans }}</h3>
        <div class="ui attached segment">
            {{ form_row(form.attachment, {'label': false}) }}
        </div>
    </div>
</div>
